public class User {
    // searchEvent(eventId: string)
    String username;
    String password;

    public String searchEvent(String eventId) {
        // call ticket class to get ticket info
        String ticketInfo = "";

        return ticketInfo;
        // test
    }

}